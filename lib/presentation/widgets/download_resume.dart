
import 'package:flutter/material.dart';

class DownloadResumeButtonAndroid extends StatelessWidget {
  const DownloadResumeButtonAndroid({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: const Text("Download Resume"),
    );
  }
}
